# SEND to File

action.sendfile = [0|1]
* Enable SEND to File

action.sendfile.param.directory = <string>
* The output directory
* (required)

action.sendfile.param.filename = <string>
* The output filename
* (required)